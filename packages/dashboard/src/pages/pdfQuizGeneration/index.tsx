import { FC } from 'react';
import { PDFQuizGeneration } from '@containers/PDFQuizGeneration';

const pdgQuizGeneration: FC = () => {
  return <PDFQuizGeneration />;
};

export default pdgQuizGeneration;
