{
  "name": "quiz-web-engine",
  "packageManager": "yarn@3.6.3",
  "license": "mit",
  "scripts": {
    "lint:eslint": "eslint packages/*",
    "lint:prettier": "prettier packages/* --check",
    "lint:types:student": "yarn workspace @quiz-web-engine/student lint:types",
    "lint:types:visitor-portal": "yarn workspace @quiz-web-engine/visitor-portal lint:types",
    "lint:types": "yarn run lint:types:student && yarn run lint:types:visitor-portal",
    "lint:format": "prettier packages/* --write"
  },
  "workspaces": [
    "packages/*",
    "packages/core/*"
  ],
  "devDependencies": {
    "eslint": "^8.45.0",
    "prettier": "^3.0.3"
  }
}
